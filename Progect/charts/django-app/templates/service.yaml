apiVersion: v1
kind: Service
metadata:
  name: web-django-app
  labels:
    app: web-django-app
spec:
  type: {{ .Values.service.type }}
  selector:
    app: web-django-app
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8000
